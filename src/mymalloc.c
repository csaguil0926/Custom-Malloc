#include <stdio.h>
#include <stdlib.h>
#include "mymalloc.h"

void *mymalloc(size_t size, char *file, int line) {
    return NULL; // Replace me with tasty code!
}
void myfree(void *ptr, char *file, int line) {
    // Replace me with tasty code!
}
